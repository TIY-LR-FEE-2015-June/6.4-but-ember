{{!-- When this form submits, call the `save` action and pass the current model --}}
<form {{action 'save' model on='submit'}}>
  {{input type='text' value=model.link placeholder="link"}}
  {{input type='text' value=model.tag placeholder="tag"}}

  <button>Submit</button>
</form>
