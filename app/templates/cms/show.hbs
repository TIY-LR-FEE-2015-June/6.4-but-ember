<h2>{{model.title}} <span>{{model.author}}</span></h2>

<p>{{model.body}}</p>

<div class="actions">
  {{#link-to 'cms.update' model._id}}
    edit
  {{/link-to}}
  <a href="#" {{action 'delete' model}}>
    delete
  </a>
</div>